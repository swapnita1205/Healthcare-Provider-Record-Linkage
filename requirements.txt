# Provider Record Linkage Pipeline — core dependencies
# Python 3.9+ recommended. Install: pip install -r requirements.txt

pandas>=1.3
numpy>=1.20
polars>=0.18
scikit-learn>=1.0
scipy>=1.7
matplotlib>=3.4
joblib>=1.1
pyarrow>=8.0

# Optional: Jupyter to run notebooks
# jupyter>=1.0
